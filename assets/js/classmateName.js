"use strict"

const ClassmateName() = () => {
  const classmateDiv = $('<div class="classmateName-conatainer"></div>');
  const formGroup = $('<div class="inputGroup"></div>');
  const labelName = $('<label for="name">Ingresa su nombre</label>');
  const inputName = $('<input type="text" name="name">');
  const button =
}
